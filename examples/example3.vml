# перегрузка подпрограммы по количеству аргументов
func swap(a, b):
    return b, a

func swap(a, b, c):
    return b, c, a

x = [1, 2, 3]
y = [4, 5]

# многоцелевой оператор присваивания
x, y = swap(x, y)  

a = 1
b = 2
c = 3
a, b, c = swap(a, b, c)